import styled from 'styled-components';

export const StyledSection = styled.section`
  width: 100%;
  height: calc(100vh - 60px);
  padding: 0 15px;
  overflow: scroll;
  margin: auto;
  text-align: center;
`;
